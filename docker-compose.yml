# docker-compose.yml

version: "3.8"

services:

  client:
    build:
      context: ./client
    command: [ "yarn", "start" ]
    container_name: afbrowser
    ports:
      - 3000:3000
    volumes:
      - ./client:/usr/src/app

  